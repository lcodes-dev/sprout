defmodule <%= @web_module %>.DashboardController do
  use <%= @web_module %>, :controller

  def index(conn, _params) do
    render(conn, :index, current_scope: conn.assigns[:current_scope])
  end
end
