defmodule <%= @web_module %>.LegalPagesController do
  use <%= @web_module %>, :controller

  def privacy(conn, _params) do
    render(conn, :privacy)
  end

  def terms(conn, _params) do
    render(conn, :terms)
  end
end
